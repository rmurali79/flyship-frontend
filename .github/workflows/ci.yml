name: CI (Linear-triggered)

# Fired by the linear-webhook-bridge Cloud Function when a Linear issue whose
# ID prefixes a branch in this repo moves to "In Progress". Validates the
# branch builds and tests cleanly — no deploy here; that only happens on
# merge to main (see deploy.yml), after a human-reviewed PR.
#
# Also fires directly on push to any Linear-ID-prefixed branch (e.g. from the
# build:claude routine, which pushes the branch only after the issue is
# already "In Progress", so the repository_dispatch above can otherwise miss
# it). This is deterministic and independent of Linear webhook timing.
on:
  repository_dispatch:
    types: [linear-in-progress]
  push:
    branches: ['FLY-*']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Report which issue/branch triggered this
        run: echo "Linear issue ${{ github.event.client_payload.issue }} -> branch ${{ github.event.client_payload.branch }}"

      - name: Checkout dispatched branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.client_payload.branch }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --watchAll=false
        env:
          CI: true

      - name: Build
        run: npm run build
